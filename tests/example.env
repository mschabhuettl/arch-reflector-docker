# Example environment configuration for Arch Reflector Docker

# Run mode:
# true: Update mirrors once and exit.
# false: Periodic updates using a cron schedule.
ONE_SHOT=false

# Cron schedule for periodic updates (5 fields format):
# Default: Every hour.
REFLECTOR_SCHEDULE=0 * * * *

# Custom arguments for the reflector command:
# Uncomment and adjust as needed.
# REFLECTOR_ARGS=--save /etc/pacman.d/mirrorlist --country Austria,Germany --protocol https --latest 10

# Timezone for the container:
# Default: UTC. Adjust to your local timezone if needed.
# TZ=Europe/Vienna

# Logging level:
# quiet: Minimal output
# normal: Standard output (default)
# debug: Detailed output for debugging
LOG_LEVEL=normal
